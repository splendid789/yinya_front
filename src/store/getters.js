export default {
    userInfo(state) {
        return state.userInfo;
    },
    userInfoAuth(state) {
        return state.userInfoAuth;
    },
    innerAudioContext(state) {
        return state.innerAudioContext;
    },
    isFirst(state) {
        return state.isFirst;
    },
    recorderManager(state) {
        return state.recorderManager;
    }
}